# autogenerated
import aws_cdk as cdk
from stack import SAMNodeJSLambda
app = cdk.App(
    default_stack_synthesizer=cdk.DefaultStackSynthesizer(
        generate_bootstrap_version_rule=False
        )
    )

SAMNodeJSLambda(app, 'Stack')
app.synth()
