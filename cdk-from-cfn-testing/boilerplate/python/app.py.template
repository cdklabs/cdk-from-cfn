# autogenerated
import aws_cdk as cdk
{{#if INCLUDE_ENV}}
import os
{{/if}}
from {{STACK_CLASS_NAME}} import {{STACK_CLASS_NAME}}

app = cdk.App(
    default_stack_synthesizer=cdk.DefaultStackSynthesizer(
        generate_bootstrap_version_rule=False
    )
)

{{#if INCLUDE_ENV}}
{{STACK_CLASS_NAME}}(app, '{{STACK_NAME}}', env=cdk.Environment(region=os.environ.get('CDK_DEFAULT_REGION') or os.environ.get('AWS_DEFAULT_REGION')))
{{else}}
{{STACK_CLASS_NAME}}(app, '{{STACK_NAME}}')
{{/if}}

app.synth()