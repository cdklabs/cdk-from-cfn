// auto-generated! a human should update this!
package com.myorg;

import software.amazon.awscdk.App;
import software.amazon.awscdk.AppProps;
import software.amazon.awscdk.DefaultStackSynthesizer;
import software.amazon.awscdk.DefaultStackSynthesizerProps;
import software.amazon.awscdk.StackProps;
{{#if INCLUDE_ENV}}
import software.amazon.awscdk.Environment;
{{/if}}

public class MyApp {
    public static void main(final String[] args) {
        App app = new App(AppProps.builder()
            .defaultStackSynthesizer(DefaultStackSynthesizer.Builder.create()
                .generateBootstrapVersionRule(false)
                .build())
            .build());

        {{#if INCLUDE_ENV}}
        new {{STACK_CLASS_NAME}}(app, "{{STACK_NAME}}", StackProps.builder()
            .env(Environment.builder()
                .region(System.getenv("CDK_DEFAULT_REGION") != null ? System.getenv("CDK_DEFAULT_REGION") : System.getenv("AWS_DEFAULT_REGION"))
                .build())
            .build());
        {{else}}
        new {{STACK_CLASS_NAME}}(app, "{{STACK_NAME}}", StackProps.builder()
            .build());
        {{/if}}

        app.synth();
    }
}